# Rust build artifacts
/rust/target/

# breadbin runtime data — downloaded, cached, or built at runtime.
# The app keeps these in its data dir (~/.breadbin); never commit them.
/covers/
/*.sqlitedb
/*.tsv
/uta_index.html

# editor / OS cruft
.#*
*~
.DS_Store
